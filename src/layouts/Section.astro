---
interface Props {
	title?: string;
	class?: string;
}

const { title, class: customClass = '' } = Astro.props;
---

<section class={`group/section py-8 ${customClass}`}>
	{title && <h3 class='mb-4'>{title}</h3>}
	<slot />
</section>
